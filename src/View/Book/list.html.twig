{% extends "layout.html.twig" %}

{% block content %}

    <h1 class="text-center">Liste des livres</h1>    

    <table class="table">
        <thead>
            <tr>
                <th>Titre</th>
                <th>Nb de pages</th>
                <th>Auteur</th>
            </tr>
        </thead>
        <tbody>
            {% for book in books %}
                <tr>
                    <td>{{ book.title }}</td>
                    <td>{{ book.nb_of_pages }}</td>
                    <td>{{ book.author }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

{% endblock %}